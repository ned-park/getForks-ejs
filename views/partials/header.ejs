<header class="header flex vertical-center">
  <a href="/">
    <% if (!user) { %>
      <h1 data-user="null">getForks</h1>
    <% } else { %>
      <h1 data-user="<%=user.username%>">getForks</h1>
    <% } %>
  </a>
  <nav>
    <input id="menu-toggle" type="checkbox" />
    <label class='menu-button-container' for="menu-toggle">
    <div class='menu-button'></div>
    </label>
    <div class="menu">
      <a href="<%=user? `/${user.username}` : `/login`%>" class="headerLink">myRecipes</a>
      <a href="/about" class="headerLink">About</a>
      <% if (!user) { %>
      <a href="/login" class="headerLink">Log in</a>
      <a href="/signup" class="headerLink">Sign up</a>
      <% } else { %>
      <a href="/logout" class="headerLink">Log out</a>
    <% } %>
    </div>
  </nav>
</header>

